# Google Search Console & Analytics 設定手順

シノテクスサイト用 / 2026年5月

---

## このドキュメントについて

Google が提供する2つの**無料**ツールをサイトに連携させる手順です。両方とも:
- **無料**(ずっと)
- **設定したら基本放置**(必要な時に見るだけ)
- **設定後の運用は楽**(ダッシュボードを開くだけ)

このドキュメントの通り進めれば30〜45分で両方完了します。

---

## 何が出来るようになるか

### Google Search Console(検索コンソール)
- 「シノテクス 坂戸市」など、どんなキーワードで検索されてサイトに来てくれてるか分かる
- 検索順位が分かる
- 検索結果に問題がある時に教えてくれる(例:このページがインデックスされてません等)
- サイトマップを送信してGoogleに「うちのサイトはこの構造です」と伝えられる
- 検索結果での表示回数、クリック数、CTR(クリック率)

### Google Analytics(GA4)
- 何人がサイトを見にきてるか(日別・月別)
- どのページが人気か
- スマホとPC、どっちで見られてるか
- どこの地域からアクセスがあるか
- どこから来たか(Googleから/直接URL入力/SNSから等)
- 訪問者がサイトに滞在した時間

---

## パート1: Google Search Console の設定

### ステップ1-1: Google アカウントの準備

通常使ってるGmailアドレスでOK(代表者個人 or 会社用 どちらでも)。**後で社員さんと共有できる**ので、後から共有設定するのも簡単。

将来的におすすめ:
- **会社専用のGmail**を作ってそれで管理(例: `shinotecs.web@gmail.com`)
- 担当者が変わっても引き継げる
- ただし最初は個人のGmailで始めても問題ない

### ステップ1-2: Search Console にアクセス

ブラウザで以下を開く:
**https://search.google.com/search-console**

ログインを求められたら、ステップ1-1で決めたGoogleアカウントでログイン。

### ステップ1-3: プロパティを追加

初めて使う場合、「**プロパティを追加**」または「**今すぐ開始**」ボタンが出ます。

「**プロパティタイプを選択**」画面で2つ表示されます:

```
ドメイン         |  URLプレフィックス
(全URLを対象)    |  (特定URLのみ)
```

→ **「URLプレフィックス」** を選択(GitHub Pagesではこちらが使いやすい)

URLを入力する欄に:
```
https://shinotecs.github.io/shinotecs-site/
```
を貼り付けて「続行」をクリック。

### ステップ1-4: 所有権の確認

「**所有権の確認**」画面が出ます。**HTMLタグ**による確認方法を選びます:

「**その他の確認方法**」を展開して「**HTML タグ**」を選択。

すると、こんなようなコードが表示されます:
```html
<meta name="google-site-verification" content="abc123xyz...." />
```

この **content="..."** の中身(英数字の文字列)を**コピー**してください。

### ステップ1-5: 私(Claude)にそのコードを送ってください

次回のメッセージで、こんな感じで送ってください:

```
Search Consoleの確認コードがこれです:
abc123xyz0987654321
```

これを受け取ったら、私が `index.html` などに必要なメタタグを追加します。

### ステップ1-6: 追加後、Search Console で「確認」ボタンを押す

私がメタタグを追加したHTMLをGitHubにアップロード後、Search Consoleの画面に戻って「**確認**」ボタンを押す。

「**所有権を証明しました**」と出れば完了。

### ステップ1-7: サイトマップを送信

Search Console のメニューから「**サイトマップ**」を選択。

「新しいサイトマップの追加」の欄に:
```
sitemap.xml
```
だけ入力して「**送信**」をクリック。

→ ステータスが「**成功**」になれば完了。

---

## パート2: Google Analytics (GA4) の設定

### ステップ2-1: Analytics にアクセス

**https://analytics.google.com/**

Search Consoleと同じGoogleアカウントでログイン。

### ステップ2-2: アカウント作成

初めての場合「**測定を開始**」ボタンが出ます。

**アカウント名**: `シノテクス株式会社`(任意)
データ共有設定はそのままチェック付きで「**次へ**」。

### ステップ2-3: プロパティ作成

**プロパティ名**: `シノテクスサイト`(任意)
レポートのタイムゾーン: `日本`
通貨: `日本円(JPY)`

「次へ」をクリック。

### ステップ2-4: ビジネス情報

- 業種カテゴリ: 「**ヘルスケア**」または「**その他**」
- ビジネスの規模: 「**小規模**」
- 利用目的: チェックボックスから該当するものを選ぶ(「**ウェブサイトのトラフィックを測定する**」を必ずチェック)

「**作成**」をクリック。

### ステップ2-5: 利用規約に同意

国を「**日本**」に変更してから、利用規約のチェックボックスにチェックを入れて「**同意する**」。

### ステップ2-6: データストリームの設定

「**ウェブ**」を選択。

**ウェブサイトのURL**: 
```
https://shinotecs.github.io/shinotecs-site/
```

**ストリーム名**: `シノテクスサイト`(任意)

「**ストリームを作成**」。

### ステップ2-7: 測定ID をコピー

画面に「**測定ID**」が表示されます。**G-XXXXXXXXXX**(Gで始まる文字列)の形式です。

これを**コピー**しておいてください。

### ステップ2-8: 私(Claude)にその測定IDを送ってください

次回のメッセージで、こんな感じで送ってください:

```
Analyticsの測定IDがこれです:
G-XXXXXXXXXX
```

これを受け取ったら、私が各HTMLにトラッキングコードを追加します。

---

## パート3: 連携設定(Search Console と Analytics をリンク)

両方の設定が終わったら、互いに連携できます。Analytics の管理画面から:

**管理**(歯車アイコン)→ **プロパティ設定**→ **製品リンク**→ **Search Console のリンク**

そこで、設定したSearch Consoleプロパティを選んで連携。これで、Analytics内で検索キーワードの情報も見られるようになります。

---

## パート4: 完了後にやること(任意)

### 数日後 ~ 1週間後

データが溜まり始めます。次のことが分かります:

**Search Console で確認:**
- 「**パフォーマンス**」セクション → どんなキーワードで検索されてるか
- 「**カバレッジ**」 → ページがちゃんとインデックスされてるか

**Analytics で確認:**
- 「**ホーム**」画面 → ユーザー数、ページビュー
- 「**レポート**」→ 「**リアルタイム**」 → 今この瞬間誰が見てるか

### 月1回くらいの運用

両方とも基本放置でOK。月に1回くらい開いて:
- ユーザー数が増えてるか
- どのページが人気か
- スマホ/PCの割合

これを見るだけで、サイト改善のヒントが得られます。

---

## つまずきポイント(よくある問題)

### Q. Search Console の「確認」が失敗する
- メタタグを `<head>` の中に正しく入れていない可能性
- GitHub に上げたか、ハードリロード(Ctrl+Shift+R)したか確認

### Q. Analytics でデータが表示されない
- 設置直後はデータが出ない(数時間〜1日待つ必要あり)
- 自分でサイトを開いてみて、「リアルタイム」で1人がカウントされるか確認

### Q. 「測定ID」が見つからない
- Analytics 管理 → データストリーム → ストリーム名をクリック → 一番上に表示

### Q. 設定を間違えた / 最初からやり直したい
- どちらも何度でもプロパティを削除して作り直せる
- 削除に料金は発生しない

---

## 困ったら

Claudeに以下のいずれかを送ってください:

- 「Search Console の確認コードを送るので、サイトに追加して」
- 「Analytics の測定IDを送るので、サイトに追加して」
- 「Search Console の連携で困ってます、スクショ送ります」

→ どんな状態でも対応できます。
